# Netlify build config (used for git-connected deploys).
# Cloudflare Pages uses the same values, set in its dashboard:
#   Build command: npm run build   ·   Build output directory: dist
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
