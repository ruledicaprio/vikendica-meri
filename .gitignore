node_modules
dist
.DS_Store
*.local
.vite

# Tooling / local-only
.claude
.wrangler
*.plugin
# Worker secrets and the local Access bypass. Never commit.
.dev.vars

# Orphaned duplicate of the photos (everything used by the site lives in public/)
data

# Generated responsive image variants (build artifacts, regenerated on demand)
.image-cache
